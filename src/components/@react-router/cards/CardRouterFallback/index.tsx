import './CardRouterFallback.scss';

export default function CardRouterFallback() {
  return (
    <article className="card-router-fallback">
      <h2 className="card-router-fallback__title">Loading...</h2>
    </article>
  );
}
